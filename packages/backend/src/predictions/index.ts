/**
 * Predictions module - validation and CRUD operations for user predictions.
 */

export { PredictionValidator } from './prediction-validator';
export type { ValidationResult } from './prediction-validator';
